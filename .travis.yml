language: python
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.5-dev"  # 3.5 development branch
  - "3.6"
  - "3.6-dev"  # 3.6 development branch
  - "3.7-dev"  # 3.7 development branch

install:
  - pip install -r test-requirements.txt

script:
  - pytest -vv --cov-fail-under 90 --cov=/xeger --cov-config {toxinidir}/.coveragerc xeger/tests
